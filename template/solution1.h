#pragma once

#include <string>
#include <vector>

namespace dayX::solution1
{
	struct Input
	{
		// parsed input data

		// functions that give solutions or solve parts
	};

	// input parsing functions
	void parse_line(std::string const& line);
	void parse_lines(std::vector<std::string> const& lines);
}
